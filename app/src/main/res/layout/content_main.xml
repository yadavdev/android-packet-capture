<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="devilrx.smartpacket.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        android:id="@+id/main_tv" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start Capture"
        android:id="@+id/button_packet_capture"
        android:onClick="startCapture"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:theme="@style/buttons" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stop and Exit"
        android:id="@+id/button_exit"
        android:onClick="stopAndExitActivity"
        android:layout_below="@+id/button_packet_capture"
        android:layout_alignLeft="@+id/button_packet_capture"
        android:layout_alignStart="@+id/button_packet_capture"
        android:layout_marginTop="37dp"
        android:layout_alignRight="@+id/button_packet_capture"
        android:layout_alignEnd="@+id/button_packet_capture"
        android:theme="@style/buttons" />
</RelativeLayout>
